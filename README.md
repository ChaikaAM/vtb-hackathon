# VTB Hackathon - API Security Analyzer

Комплексный инструмент для автоматического анализа уязвимостей API, объединяющий **статический анализ**, **динамическое тестирование** и **валидацию контракта** в одном решении.

## Возможности

- **Статический анализ** - анализ OpenAPI спецификаций
- **Динамическое тестирование** - тестирование реального API на уязвимости
- **Валидация контракта** - сравнение поведения API с спецификацией
- **Интеграция с ИИ** - анализ и фильтрация уязвимостей с помощью AI агента
- **Покрытие OWASP API Top 10 2023**
- **Генерация отчетов** - HTML, JSON

##  Архитектура

```
vtbhackathon/
├── frontend/          # React приложение
│   ├── src/
│   │   ├── App.js    # UI для запуска анализа
│   │   └── App.css   # Стили
│   └── package.json
│
├── backend/           # Spring Boot API
│   ├── src/main/java/com/vtb/apisecurity/
│   │   ├── controller/      # REST API
│   │   ├── service/         # Бизнес-логика
│   │   │   ├── rules/      # OWASP правила
│   │   │   └── detectors/  # Детекторы уязвимостей
│   │   ├── model/          # Модели данных
│   │   └── config/         # Конфигурация
│   └── pom.xml
│
└── architecture/      # Документация и спецификации
    ├── defense.md
    ├── owasp-api-top10-2023-complete.md
    └── *-openapi.json # Тестовые API
```

## Быстрый старт

### Backend (Java 17+)

```bash
cd backend
./mvnw clean install
./mvnw spring-boot:run
```

Backend будет доступен: `http://localhost:8080/api`

### Frontend (Node.js)

```bash
cd frontend
npm install
npm start
```

Frontend будет доступен: `http://localhost:3000`

## Пример отчета

После анализа вы получите:

- Список всех обнаруженных уязвимостей
- Классификация по OWASP API Top 10
- Уровень критичности (CRITICAL, HIGH, MEDIUM, LOW)
- Конкретные рекомендации по исправлению
- Несоответствия между спецификацией и реализацией

## ИИ Агент

Инструмент интегрирован с AI агентом для:
- Фильтрации ложных срабатываний
- Углубленного анализа уязвимостей
- Генерации персонализированных рекомендаций

## 📝 Примеры использования

### Анализ через UI

1. Откройте http://localhost:3000
2. Выберите предустановленный API или введите свой
3. Нажмите "Запустить анализ"
4. Просмотрите результаты